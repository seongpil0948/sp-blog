import { tv } from '@nextui-org/theme'

const switchStyle = tv({
  slots: {
    wrapper:
      'flex items-center justify-center h-8 w-8 rounded-lg bg-default-100 hover:bg-default-200',
  },
})

export { switchStyle }
